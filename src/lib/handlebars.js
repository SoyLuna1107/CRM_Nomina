const Handlebars = require("handlebars");

// Handlebars.registerHelper("isAdministradorSeleccion", (value) => {
//   return value === "Administrador" || value === "Seleccion";
// });

// Handlebars.registerHelper("isSite7", (value) => {
//   return value === "Site 7";
// });